import React from 'react';
import Home from './home.jsx';

export default function PatientDashboard() {
  // For now, the patient dashboard is the same as the home component
  // This gives us flexibility to customize it later
  return <Home />;
}
